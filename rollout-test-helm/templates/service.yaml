apiVersion: v1
kind: Service
metadata:
  name: {{ include "rollout-test-helm.fullname" . }}
  labels:
    {{- include "rollout-test-helm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "rollout-test-helm.selectorLabels" . | nindent 4 }}
